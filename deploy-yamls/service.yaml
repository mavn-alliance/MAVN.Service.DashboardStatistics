apiVersion: v1
kind: Service
metadata:
  labels:
    app: dashboardstatistics
    environment: Development
  name: dashboardstatistics
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: dashboardstatistics
  sessionAffinity: None
  type: ClusterIP
